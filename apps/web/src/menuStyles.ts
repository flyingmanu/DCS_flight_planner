import type { CSSProperties } from "react";

export const menuItemClassName = "dfp-menu-item";

export const menuPanelClassName = "dfp-menu-panel";

export const submenuPanelStyle: CSSProperties = {
  position: "absolute",
  left: "100%",
  top: 0,
  minWidth: 220,
};
